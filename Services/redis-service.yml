apiVersion: v1
kind: Service
metadata:
  name: redis-slave
  labels:
    app: guestbook-app
    role: slave
    tier: backend
spec:
  ports:
  - port: 6379
  selector:
    app: guestbook-app
    role: slave
    tier: backend
